# Required for OpenRouter routing
OPENROUTER_API_KEY=sk-or-v1-...

# Optional overrides (also writable via `cue config set`)
# CUE_DAEMON_BIND=127.0.0.1
# CUE_DAEMON_PORT=7821
# CUE_DEFAULT_AGENT=interview-behavioral
